# Task 6 - ASL Letter Classification using Random Forest (our custom trained model)
# Loads the trained Random Forest model from model/ and predicts the ASL letter
# (A-Z) from the 63-dimensional MediaPipe landmark feature vector.
